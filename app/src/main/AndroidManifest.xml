<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fernanda.trabalho1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.LivroActivity" />
        <activity android:name=".ui.PessoaActivity" />
        <activity android:name=".ui.CadastroPessoaActivity"></activity>
        <activity android:name=".ui.CadastroLivroActivity"></activity>
        <activity android:name=".ui.CadastroReservaActivity"></activity>
    </application>

</manifest>